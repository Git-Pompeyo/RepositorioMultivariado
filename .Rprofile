source("renv/activate.R")

if (interactive()) {
  suppressMessages({
    library(tidyverse)
    library(here)
    library(krulRutils)
    library(ISLR2)
    library(patchwork)
    library(GGally)
    library(janitor)
  })
}
